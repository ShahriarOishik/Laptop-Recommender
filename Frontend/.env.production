VITE_API_BASE_URL=https://kruger-ads-average-champions.trycloudflare.com
# true only for offline/demo work against the built-in mock engine.
# The production/default flow talks to the real FastAPI backend.
VITE_USE_MOCK_API=false
